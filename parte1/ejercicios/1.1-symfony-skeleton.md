# Ejercicio 1.1: Crear el Symfony Skeleton

## Objetivo

Inicializar el wallet-service con las dependencias correctas y herramientas de calidad configuradas.

## Instrucciones

### Paso 1: Crear el proyecto

```bash
cd ~/fsc-php-curso
symfony new wallet-service --version=7.4.* --no-git
cd wallet-service
```

### Paso 2: Instalar dependencias de desarrollo

Instala con `composer require --dev` los paquetes de la plantilla de la
Parte 1: PHPUnit 11, PHPStan, la extensión phpstan-symfony, CS-Fixer
(`php-cs-fixer/shim`), PHPUnit Bridge y Maker Bundle (opcional).

> **Dudas sobre nombres o versiones exactas?** Están en la lección 1.1 y en
> la solución al final de este ejercicio, que incluye el `composer.json`
> completo.

### Paso 3: Crear phpunit.xml.dist

Crear el archivo con la configuración de la plantilla de la Parte 1.

### Paso 4: Crear phpstan.neon

Configúralo con nivel 6, paths `src` y `tests`, e incluyendo la extensión
`phpstan-symfony` (ver la solución al final del ejercicio).

### Paso 5: Crear .php-cs-fixer.dist.php

Configurar con reglas `@PHP83Migration` y `@Symfony`.

### Paso 6: Crear Makefile

Targets: test, stan, fix, fix-check, ci, serve, clean, all, help.

### Paso 7: Verificar

```bash
make ci
# Debe pasar sin errores
```

## Entregable

wallet-service/ con make ci pasando.

## Puntos

| Criterio | Puntos |
|---|---|
| Proyecto creado con Symfony 7.4 | 1 |
| Dependencias instaladas correctamente | 1 |
| phpunit.xml.dist configurado | 1 |
| phpstan.neon nivel 6 | 1 |
| CS-Fixer configurado | 1 |
| Makefile con targets esenciales | 1 |
| make ci pasa | 1 |
| **Total** | **7** |
